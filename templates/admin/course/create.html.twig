{% extends 'adminBase.html.twig' %}
{% block body %}
    {{ parent() }}
    <div class="container">
        <h1>Create course</h1>
        {% if error %}
            <div class="alert alert-danger" role="alert">
                {{ error }}
            </div>
        {% endif %}
        {{ form(form) }}
    </div>
{% endblock %}