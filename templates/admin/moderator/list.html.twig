{% extends 'adminBase.html.twig' %}

{% block body %}
    {{ parent() }}
    <div class="container">
        <h1>Moderators</h1>
        {{ component('EntityList', {
            headers: headers,
            entities: moderators,
            actions: actions
        }) }}
    </div>
{% endblock %}